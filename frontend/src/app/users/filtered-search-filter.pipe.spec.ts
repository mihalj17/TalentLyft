import { FilteredSearchFilterPipe } from './filtered-search-filter.pipe';

describe('FilteredSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteredSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
